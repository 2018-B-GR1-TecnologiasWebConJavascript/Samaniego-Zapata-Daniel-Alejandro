const rxjs = require('rxjs');
const map = require('rxjs/operators').map;
const promesa, { return: , new: Promise };
((resolve, reject) => {
    if (Sg) {
        resolve('ok');
    }
    else {
        reject('mal');
    }
});
console.log(promesa);
//const promesaObservable = rxjs.from(promesa(true));
